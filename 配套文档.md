# 昌乐幼·儿童观察记录平台

## 创 AI 案例配套资源

---

**案例类型**：智能信息系统
**版本号**：v1.0.0
**开发时间**：2026年3月—5月
**开发者**：昌乐幼儿园信息化团队
**技术栈**：微信小程序 + Node.js 后端 + MySQL 数据库
**在线访问**：https://aixint.cn

---

## 第一部分：开发与应用报告

> 本报告参照《创AI案例开发与应用报告》模板撰写，字数约3000字。

### 一、开发背景

#### （一）问题发现

昌乐幼儿园在日常教学中，教师需要持续观察并记录每位幼儿在各个发展领域的表现。传统纸质记录方式存在以下突出问题：

1. **记录分散，难以汇总**：每位幼儿的观察记录散落在不同教师的纸质笔记本中，难以形成完整的儿童发展档案。
2. **分析困难，缺乏标准**：教师依靠个人经验对幼儿进行评价，缺乏统一的标准参照，无法保证评价的客观性和一致性。
3. **报告生成耗时**：学期末需要人工整理大量记录才能形成成长报告，工作量巨大且容易出错。
4. **家园沟通效率低**：家长无法及时了解幼儿在园的发展情况，家园共育缺乏有效的信息化支撑。
5. **数据价值未充分挖掘**：大量历史观察数据未被有效利用，无法为教育决策提供数据支撑。

#### （二）需求分析

针对以上问题，我们进行了详细的需求分析：

- **教师端**：需要一个便捷的移动端工具，能够随时随地记录幼儿观察信息，支持图片上传，支持按领域和维度进行结构化评价。
- **管理端**：需要能够汇总全园数据，生成统计报表，为教育决策提供依据。
- **家长端**：需要能够通过微信直接查看幼儿的成长报告，无需下载额外App。
- **标准依据**：评价体系必须基于《3-6岁儿童学习与发展纲要》，确保评价的科学性和权威性。

#### （三）AI辅助开发引入

在项目开发过程中，我们引入了**生成式人工智能**（豆包/MiniMax）辅助开发，主要用于：

- 项目架构设计与技术选型建议
- 数据库表结构设计
- API路由与业务逻辑设计
- Canvas图表绘制算法实现
- 领域评价体系的数据结构设计
- 代码审查与性能优化建议

AI工具的引入大幅降低了开发门槛，提高了开发效率，使我们能够在较短时间内完成一个功能完整的幼儿园信息管理系统。

---

### 二、设计与开发

#### （一）系统总体架构

本系统采用前后端分离架构，前端为微信小程序，后端为Node.js服务，数据库为MySQL。

```
┌──────────────────────────────────────┐
│          微信小程序（教师/家长端）      │
│   pages: login, home, record,        │
│   detail, report, export              │
└─────────────┬────────────────────────┘
              │ HTTPS API
┌─────────────▼────────────────────────┐
│      Node.js + Koa2 后端服务          │
│  端口: 3000                          │
│  功能: 认证、CRUD、报表生成、AI分析     │
└─────────────┬────────────────────────┘
              │ SQL
┌─────────────▼────────────────────────┐
│          MySQL 数据库                 │
│  users / observations 表              │
└──────────────────────────────────────┘
```

#### （二）技术选型理由

| 组件 | 选型 | 理由 |
|-----|------|------|
| 前端 | 微信小程序 | 教师/家长使用微信，无需额外App，低门槛 |
| 后端 | Node.js + Koa2 | 轻量、异步IO适合IO密集型业务 |
| 数据库 | MySQL | 成熟稳定，支持JSON类型便于存储分析数据 |
| 图表 | Canvas原生绘制 | 无需引入第三方库，减少包体积 |
| AI | 豆包/MiniMax API | 辅助代码生成与评审 |

#### （三）数据库设计

**users 表（用户表）**

| 字段 | 类型 | 说明 |
|-----|------|-----|
| id | INT | 主键，自增 |
| openid | VARCHAR(64) | 微信openid，唯一 |
| name | VARCHAR(50) | 用户名称 |
| avatar | VARCHAR(255) | 头像URL |
| role | ENUM | teacher/admin |
| created_at | DATETIME | 创建时间 |

**observations 表（观察记录表）**

| 字段 | 类型 | 说明 |
|-----|------|-----|
| id | INT | 主键，自增 |
| user_id | INT | 外键→users |
| openid | VARCHAR(64) | 微信openid |
| record_type | ENUM | individual/event |
| class_name | VARCHAR(20) | 被观察幼儿班级 |
| target_name | VARCHAR(50) | 被观察对象姓名 |
| observation_date | DATETIME | 观察时间 |
| content | TEXT | 观察内容 |
| analysis | TEXT | 综合分析 |
| analysis_data | JSON | 领域维度评级数据 |
| images | JSON | 图片URL数组 |
| linked_id | VARCHAR(64) | 串联观察ID |
| created_at | DATETIME | 创建时间 |

#### （四）核心功能开发

**1. 微信授权登录**
- 使用 wx.login() 获取临时code
- 后端用code换取openid，签发JWT token
- 前端携带token访问受保护接口

**2. 六大领域评价体系**
- 基于《3-6岁儿童学习与发展纲要》设计
- 6个领域 × 多个维度 × 4级评级（优秀/良好/一般/需关注）
- 评级数据以JSON格式存储，便于后续统计分析

**3. Canvas雷达图绘制**
- 纯原生Canvas API实现，无第三方依赖
- 支持6维度数据可视化
- 根据评级自动计算分值（优秀=5,良好=4,一般=3,需关注=2）

**4. 成长报告自动生成**
- 汇聚同一幼儿多次观察记录
- 按领域汇总评级，计算综合评分
- 生成雷达图+趋势图+AI评语+家庭建议

**5. 导出功能**
- 支持Excel和Word格式
- 支持按记录类型/日期范围/串联ID精准导出

#### （五）AI辅助开发过程（关键证据）

本项目在开发过程中**全程借助生成式人工智能**进行辅助，主要环节如下：

**环节1：项目初始化提示词**

```
我想开发一个微信小程序，用于幼儿园儿童观察记录。
功能：登录、记录创建、领域选择评级、图片上传、成长报告生成、导出
技术：微信小程序+Node.js+MySQL
请帮我设计项目结构和核心文件。
```

**环节2：Canvas雷达图提示词**

```
我需要在微信小程序中使用Canvas绘制雷达图，展示儿童发展评测结果。
要求：6个维度、5个等级、同心圆背景、数据多边形、轴线标签
请用微信小程序Canvas API实现，附完整代码。
```

**环节3：领域数据结构设计提示词**

```
请设计幼儿园儿童发展观察记录的领域评价数据。
基于《3-6岁儿童学习与发展纲要》，6个领域（健康/语言/社会/科学/艺术/学习品质），
每个维度支持4个评级：优秀/良好/一般/需关注。
用JavaScript对象形式给出数据结构。
```

**环节4：后端API设计提示词**

```
请为幼儿园观察记录系统设计后端API（Node.js+Koa2+MySQL）。
端点：登录、CRUD观察记录、串联记录查询、报告生成、Excel/Word导出
请设计SQL建表语句和关键路由逻辑。
```

> 以上AI辅助开发的完整提示词与回复记录，已整理收录于本配套资源第五部分。

---

### 三、应用过程与效果

#### （一）部署与上线

- 2026年5月：系统完成开发并部署至生产服务器（8.134.189.98）
- 域名 https://aixint.cn 正式上线
- 微信小程序通过审核并发布

#### （二）应用规模

- **覆盖班级**：小班1班、小班2班、中班1班、中班2班、大班1班、大班2班（共6个班级）
- **使用对象**：全园教师及园长
- **主要功能使用情况**：记录创建、报告生成、导出使用频率最高

#### （三）解决的问题

| 问题 | 解决方案 | 成效 |
|-----|---------|------|
| 纸质记录分散 | 电子化集中存储，统一管理 | 记录查找效率提升 |
| 评价标准不统一 | 内置六大领域标准化评级体系 | 评价客观性提高 |
| 成长报告耗时 | 一键自动生成含雷达图的报告 | 报告生成时间从数小时降至几秒 |
| 家园沟通不畅 | 微信直接查看报告 | 家长满意度提升 |
| 数据难以分析 | 雷达图+趋势图可视化 | 幼儿发展情况一目了然 |

#### （四）创新亮点

1. **评价体系标准化**：严格对标《3-6岁儿童学习与发展纲要》，六大领域全覆盖，评价有据可依。
2. **移动端优先**：以微信小程序为载体，教师随时随地记录，家长随时随地查看。
3. **Canvas原生图表**：不依赖第三方图表库，雷达图和趋势图完全自研，减少小程序包体积。
4. **串联观察机制**：通过linked_id实现同一幼儿多次观察的串联，方便追踪发展轨迹。
5. **AI辅助开发实践**：全程借助国产大模型辅助开发，是一次教师利用AI工具提升信息化能力的典型案例。

---

### 四、创新与反思

#### （一）创新点总结

1. **应用场景创新**：将AI辅助开发与幼儿教育评价结合，为一线教师提供可复制的AI工具开发范例。
2. **技术实现创新**：纯Canvas实现雷达图和趋势图，无外部依赖，轻量化运行。
3. **业务流程创新**：串联观察机制让零散的记录形成完整的儿童发展档案。

#### （二）不足与改进方向

1. **当前不足**：暂未接入国产大模型API进行AI自动评语生成（目前评语为模板化建议）。
2. **改进方向**：下一步计划接入DeepSeek/智谱清言等国产大模型，实现基于观察内容的AI自动分析评语生成。
3. **扩展计划**：增加家长主动查询模块、数据对比分析、学期报告自动汇总等功能。

#### （三）可复制性与推广价值

本系统的设计思路和技术方案具有较强的可复制性，适用于：
- 其他幼儿园的儿童观察记录信息化
- 中小学教师的课堂观察记录
- 特殊教育学校的儿童发展评估

---

## 第二部分：使用手册

### 2.1 系统简介

**昌乐幼·儿童观察记录平台**是一款面向幼儿园教师和园长的儿童发展观察记录工具，基于《3-6岁儿童学习与发展纲要》设计，对幼儿在健康、语言、社会、科学、艺术五大领域及学习品质进行系统性观察和记录。

### 2.2 功能模块

| 模块 | 功能说明 |
|-----|---------|
| 登录 | 微信授权登录，自动获取用户信息 |
| 首页 | 记录列表、搜索筛选、新建入口 |
| 记录创建/编辑 | 领域选择、维度评级、图片上传 |
| 详情页 | 记录查看、串联观察、报告生成 |
| 成长报告 | 雷达图、趋势图、AI评语、家庭建议 |
| 导出 | Excel/Word格式导出 |

### 2.3 使用流程

**创建记录**：`登录 → 首页"+" → 填写信息 → 选择领域和评级 → 上传图片 → 保存`

**生成报告**：`详情页 → "一键生成成长报告" → 等待分析 → 查看/分享`

### 2.4 领域评价体系

| 序号 | 领域 | 维度数 | 评级 |
|-----|------|-------|------|
| 1 | 健康领域 | 4个 | 优秀/良好/一般/需关注 |
| 2 | 语言领域 | 4个 | 优秀/良好/一般/需关注 |
| 3 | 社会领域 | 5个 | 优秀/良好/一般/需关注 |
| 4 | 科学领域 | 4个 | 优秀/良好/一般/需关注 |
| 5 | 艺术领域 | 3个 | 优秀/良好/一般/需关注 |
| 6 | 学习品质 | 4个 | 优秀/良好/一般/需关注 |

---

## 第三部分：安装手册

### 3.1 开发环境要求

| 组件 | 要求 |
|-----|------|
| Node.js | >= 14.x |
| 微信开发者工具 | 最新版 |
| MySQL | 5.7+ |
| Git | 任意版本 |

### 3.2 前端安装步骤

**步骤1：克隆仓库**

```bash
git clone https://github.com/forever361/child_info_platform.git
cd child_info_platform
npm install
```

**步骤2：导入项目**

打开微信开发者工具 → 导入项目 → 选择目录 → 填写AppID

**步骤3：配置合法域名**

登录微信公众平台，添加request合法域名：
- `https://aixint.cn`
- `https://upload.aixint.cn`

添加uploadFile合法域名：
- `https://upload.aixint.cn`

**步骤4：修改API地址（如需对接自己的后端）**

修改 `utils/api.js` 中的 API_BASE 和 UPLOAD_BASE。

### 3.3 后端安装步骤

```bash
git clone https://github.com/forever361/server_child_info.git
cd server_child_info
npm install
# 配置 .env 文件（PORT、DB、JWT_SECRET等）
npm start
```

### 3.4 生产服务器信息

- IP：8.134.189.98
- 用户：realtime
- 服务端口：3000

---

## 第四部分：完整代码（约2000行）

### 4.1 项目结构

```
child_info_platform/
├── app.js / app.json / app.wxss
├── project.config.json / package.json
├── pages/
│   ├── login/    (login.js 72行)
│   ├── home/     (home.js 142行)
│   ├── record/   (record.js 331行)
│   ├── detail/   (detail.js 155行)
│   ├── report/   (report.js 144行)
│   └── export/   (export.js 131行)
├── utils/
│   ├── api.js         (111行)
│   ├── chart-radar.js  (104行)
│   └── chart-trend.js (183行)
└── images/
```

**代码总量**：JS约1389行 + WXML约467行 + WXSS约1248行 ≈ **3100行**

### 4.2 核心代码

#### app.json（全局配置）

```json
{
  "pages": [
    "pages/login/login",
    "pages/home/home",
    "pages/record/record",
    "pages/detail/detail",
    "pages/export/export",
    "pages/report/report"
  ],
  "window": {
    "backgroundTextStyle": "light",
    "navigationBarBackgroundColor": "#4A90D9",
    "navigationBarTitleText": "昌乐幼 - 观察记录",
    "navigationBarTextStyle": "white"
  }
}
```

#### utils/api.js（API封装，111行）

```javascript
const API_BASE = 'https://aixint.cn/api';
const UPLOAD_BASE = 'https://upload.aixint.cn/api';
const getToken = () => wx.getStorageSync('token');

const request = (options) => {
  return new Promise((resolve, reject) => {
    const token = getToken();
    const header = { 'Content-Type': 'application/json', ...options.header };
    if (token) header.Authorization = `Bearer ${token}`;
    wx.request({
      url: API_BASE + options.url,
      method: options.method || 'GET',
      data: options.data,
      header,
      success: (res) => {
        if (res.statusCode === 200) resolve(res.data);
        else if (res.statusCode === 401) {
          wx.removeStorageSync('token');
          wx.redirectTo({ url: '/pages/login/login' });
          reject(new Error('未授权'));
        } else reject(new Error(res.data.error || '请求失败'));
      },
      fail: reject
    });
  });
};

export const login = (code, userInfo) => request({ url: '/auth/login', method: 'POST', data: { code, userInfo } });
export const getObservations = (params) => request({ url: '/observations', data: params });
export const getObservation = (id) => request({ url: `/observations/${id}` });
export const getLinkedObservations = (linkedId) => request({ url: `/observations/linked/${linkedId}` });
export const uploadImage = (filePath) => {
  return new Promise((resolve, reject) => {
    const token = getToken();
    wx.uploadFile({
      url: UPLOAD_BASE + '/observations/upload', filePath, name: 'image',
      header: { Authorization: `Bearer ${token}` },
      success: (res) => {
        try {
          const result = JSON.parse(res.data);
          result.url ? resolve(result.url) : reject(new Error(result.error || '上传失败'));
        } catch { reject(new Error('解析响应失败')); }
      },
      fail: () => reject(new Error('上传失败'))
    });
  });
};
export const createObservation = (data, imagePaths) => {
  if (imagePaths && imagePaths.length > 0) {
    return new Promise((resolve, reject) => {
      Promise.all(imagePaths.map(path => uploadImage(path)))
        .then((urls) => request({ url: '/observations', method: 'POST', data: { ...data, images: JSON.stringify(urls) } }))
        .then(resolve).catch(reject);
    });
  }
  return request({ url: '/observations', method: 'POST', data });
};
export const updateObservation = (id, data) => request({ url: `/observations/${id}`, method: 'PUT', data });
export const deleteObservation = (id) => request({ url: `/observations/${id}`, method: 'DELETE' });
export const exportExcel = (params) => request({ url: '/export/excel', method: 'POST', data: params });
export const exportWord = (params) => request({ url: '/export/word', method: 'POST', data: params });
export const generateReport = (targetName, className) => request({ url: '/report/generate', method: 'GET', data: { target_name: targetName, class_name: className } });
export default { login, getObservations, getObservation, getLinkedObservations, createObservation, updateObservation, deleteObservation, exportExcel, exportWord, uploadImage, generateReport };
```

#### utils/chart-radar.js（Canvas雷达图，104行）

```javascript
const LEVEL_MAP = { '优秀': 5, '良好': 4, '一般': 3, '需关注': 2 };
function drawRadarChart(canvasId, domainSummary, canvasWidth) {
  if (!canvasId || !domainSummary || !domainSummary.length) return;
  const ctx = wx.createCanvasContext(canvasId);
  const width = canvasWidth || 320, height = 320;
  const centerX = width / 2, centerY = height / 2 - 15, radius = width * 0.28;
  const domains = domainSummary.map(d => {
    const full = d.domainLabel || '';
    if (full.includes('健康')) return '健康'; if (full.includes('语言')) return '语言';
    if (full.includes('社会')) return '社会'; if (full.includes('科学')) return '科学';
    if (full.includes('艺术')) return '艺术'; if (full.includes('学习')) return '学习品质';
    return full;
  });
  const n = domains.length, angleStep = (2 * Math.PI) / n;
  // Background rings
  ctx.setStrokeStyle('#e5e7eb'); ctx.setLineWidth(1);
  for (let ring = 1; ring <= 5; ring++) {
    const r = radius * (ring / 5);
    ctx.beginPath();
    for (let i = 0; i < n; i++) {
      const angle = i * angleStep - Math.PI / 2;
      const x = centerX + r * Math.cos(angle), y = centerY + r * Math.sin(angle);
      if (i === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y);
    }
    ctx.closePath(); ctx.stroke();
  }
  // Axes
  ctx.setStrokeStyle('#d1d5db');
  for (let i = 0; i < n; i++) {
    const angle = i * angleStep - Math.PI / 2;
    ctx.beginPath();
    ctx.moveTo(centerX, centerY);
    ctx.lineTo(centerX + radius * Math.cos(angle), centerY + radius * Math.sin(angle));
    ctx.stroke();
  }
  // Data polygon
  const dataPoints = domainSummary.map((d, i) => {
    const level = LEVEL_MAP[d.level] || 3, r = radius * (level / 5);
    const angle = i * angleStep - Math.PI / 2;
    return { x: centerX + r * Math.cos(angle), y: centerY + r * Math.sin(angle) };
  });
  ctx.beginPath();
  dataPoints.forEach((p, i) => { if (i === 0) ctx.moveTo(p.x, p.y); else ctx.lineTo(p.x, p.y); });
  ctx.closePath();
  ctx.setFillStyle('rgba(74, 144, 217, 0.25)');
  ctx.setStrokeStyle('#4A90D9'); ctx.setLineWidth(2);
  ctx.fill(); ctx.stroke();
  dataPoints.forEach(p => {
    ctx.beginPath(); ctx.arc(p.x, p.y, 4, 0, 2 * Math.PI);
    ctx.setFillStyle('#4A90D9'); ctx.fill();
  });
  // Labels
  const labelRadius = radius + 24;
  const colors = ['#10b981', '#3b82f6', '#8b5cf6', '#f59e0b', '#ef4444', '#06b6d4'];
  domains.forEach((label, i) => {
    const angle = i * angleStep - Math.PI / 2;
    const x = centerX + labelRadius * Math.cos(angle), y = centerY + labelRadius * Math.sin(angle);
    ctx.setFontSize(10); ctx.setFillStyle(colors[i] || '#666');
    ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
    ctx.fillText(label, x, y);
  });
  ctx.draw();
}
module.exports = { drawRadarChart, LEVEL_MAP };
```

#### pages/record/record.js（核心业务，331行）

```javascript
const api = require('../../utils/api.js');
// 六大领域数据（对应《3-6岁儿童学习与发展纲要》）
const DOMAIN_DATA = {
  1: { label: '健康领域（含动作与生活）', levels: ['优秀', '良好', '一般', '需关注'],
    dimensions: [
      { name: '生活自理', desc: '进餐、盥洗、穿衣、整理物品等习惯' },
      { name: '大肌肉动作', desc: '走、跑、跳、钻、爬、平衡、投掷' },
      { name: '精细动作', desc: '握笔、折纸、串珠、使用工具、手眼协调' },
      { name: '安全与健康', desc: '自我保护意识、情绪稳定、适应集体' }
    ]},
  2: { label: '语言领域', levels: ['优秀', '良好', '一般', '需关注'],
    dimensions: [
      { name: '倾听理解', desc: '能认真听、听懂指令、理解日常与故事内容' },
      { name: '表达交流', desc: '口齿清晰、词汇丰富、能完整讲述与交流' },
      { name: '早期阅读', desc: '对图书感兴趣、理解画面、有阅读习惯' },
      { name: '文明用语', desc: '主动问候、礼貌表达、恰当使用语言' }
    ]},
  3: { label: '社会领域', levels: ['优秀', '良好', '一般', '需关注'],
    dimensions: [
      { name: '自我意识', desc: '情绪管理、自信表达、适应集体生活' },
      { name: '人际交往', desc: '主动交往、友好合作、愿意分享、帮助他人' },
      { name: '冲突处理', desc: '能协商解决、不攻击、懂得等待与轮流' },
      { name: '规则意识', desc: '遵守班级常规、游戏规则、活动秩序' },
      { name: '归属感', desc: '喜爱班级、喜爱幼儿园，尊重师长同伴' }
    ]},
  4: { label: '科学领域（探究与认知）', levels: ['优秀', '良好', '一般', '需关注'],
    dimensions: [
      { name: '探究兴趣', desc: '好奇好问、喜欢观察、乐于尝试' },
      { name: '数感与认知', desc: '数量、形状、比较、排序、对应等经验' },
      { name: '逻辑思维', desc: '简单分类、推理、发现事物关系' },
      { name: '自然感知', desc: '对动植物、天气、材料等自然现象感兴趣' }
    ]},
  5: { label: '艺术领域（感受与表现）', levels: ['优秀', '良好', '一般', '需关注'],
    dimensions: [
      { name: '音乐感受', desc: '喜欢唱歌、律动，感受节奏旋律' },
      { name: '美术表现', desc: '愿意涂画、手工创作，大胆表达想象' },
      { name: '审美情趣', desc: '欣赏美、愿意表现，参与艺术活动' }
    ]},
  6: { label: '学习品质（专注力+习惯）', levels: ['优秀', '良好', '一般', '需关注'],
    dimensions: [
      { name: '专注力', desc: '活动持续时间、抗干扰、不分心' },
      { name: '任务意识', desc: '能坚持完成任务、不轻易放弃' },
      { name: '学习习惯', desc: '积极参与、大胆尝试、认真操作' },
      { name: '独立性', desc: '自主思考、自主解决简单问题' }
    ]}
};
Page({
  data: {
    id: null, linkedId: null, recordType: 'individual', targetName: '', classIndex: 0,
    classList: ['小班1班', '小班2班', '中班1班', '中班2班', '大班1班', '大班2班'],
    dateIndex: [0, 0, 0, 0, 0], dateList: [[], [], [], [], []],
    observationDate: null, observationDateStr: '',
    domainIndex: 0,
    domainList: ['请选择领域', '健康领域', '语言领域', '社会领域', '科学领域', '艺术领域', '学习品质'],
    domainLabels: ['', '健康领域（含动作与生活）', '语言领域', '社会领域', '科学领域（探究与认知）', '艺术领域（感受与表现）', '学习品质（专注力+习惯）'],
    levelList: ['优秀', '良好', '一般', '需关注'], currentDimensions: [], analysis: '', images: [], isEdit: false, loading: false
  },
  onLoad(options) {
    this.initDatePicker();
    if (options.id) { this.setData({ id: options.id, isEdit: true }); this.loadData(options.id); }
    if (options.linkedId) this.setData({ linkedId: options.linkedId });
    if (options.targetName) this.setData({ targetName: decodeURIComponent(options.targetName) });
    if (options.className) { const idx = this.data.classList.indexOf(decodeURIComponent(options.className)); if (idx >= 0) this.setData({ classIndex: idx }); }
  },
  initDatePicker() {
    const now = new Date(), years = [], months = [], days = [], hours = [], minutes = [];
    for (let y = 2020; y <= 2030; y++) years.push(String(y));
    for (let m = 1; m <= 12; m++) months.push(String(m).padStart(2, '0'));
    for (let d = 1; d <= 31; d++) days.push(String(d).padStart(2, '0'));
    for (let h = 0; h <= 23; h++) hours.push(String(h).padStart(2, '0'));
    for (let m = 0; m <= 59; m++) minutes.push(String(m).padStart(2, '0'));
    const dateList = [years, months, days, hours, minutes];
    const dateIndex = [years.indexOf(String(now.getFullYear())), months.indexOf(String(now.getMonth() + 1).padStart(2, '0')), days.indexOf(String(now.getDate()).padStart(2, '0')), hours.indexOf(String(now.getHours()).padStart(2, '0')), minutes.indexOf(String(now.getMinutes()).padStart(2, '0'))];
    const observationDateStr = now.getFullYear() + '-' + String(now.getMonth() + 1).padStart(2, '0') + '-' + String(now.getDate()).padStart(2, '0') + ' ' + String(now.getHours()).padStart(2, '0') + ':' + String(now.getMinutes()).padStart(2, '0');
    this.setData({ dateList, dateIndex, observationDate: now, observationDateStr });
  },
  onDateChange(e) {
    const { dateList } = this.data;
    const [y, mo, d, h, mi] = e.detail.value;
    const dateStr = dateList[0][y] + '-' + dateList[1][mo] + '-' + dateList[2][d] + ' ' + dateList[3][h] + ':' + dateList[4][mi];
    this.setData({ dateIndex: e.detail.value, observationDate: new Date(dateStr.replace(' ', 'T')), observationDateStr: dateStr });
  },
  onClassChange(e) { this.setData({ classIndex: e.detail.value }); },
  onTypeChange(e) { this.setData({ recordType: e.detail.value }); },
  onDomainChange(e) {
    const index = parseInt(e.detail.value);
    let currentDimensions = [];
    if (index > 0 && DOMAIN_DATA[index]) {
      currentDimensions = DOMAIN_DATA[index].dimensions.map(dim => ({ ...dim, levelIndex: 0, level: '优秀' }));
    }
    this.setData({ domainIndex: index, currentDimensions });
  },
  onDimensionChange(e) {
    const { index } = e.currentTarget.dataset;
    const levelIndex = parseInt(e.detail.value);
    const currentDimensions = [...this.data.currentDimensions];
    currentDimensions[index].levelIndex = levelIndex;
    currentDimensions[index].level = this.data.levelList[levelIndex];
    this.setData({ currentDimensions });
  },
  onInput(e) { this.setData({ [e.currentTarget.dataset.field]: e.detail.value }); },
  async loadData(id) {
    try {
      const res = await api.getObservation(id);
      const obs = res.observation;
      let images = obs.images;
      if (typeof images === 'string') { try { images = JSON.parse(images || '[]'); } catch { images = []; } }
      const IMAGE_BASE = 'https://aixint.cn';
      images = (images || []).map(img => img.startsWith('http') ? img : IMAGE_BASE + img);
      let analysisData = null, domainIndex = 0, currentDimensions = [];
      if (obs.analysis_data) {
        try {
          analysisData = JSON.parse(obs.analysis_data);
          domainIndex = analysisData.domainIndex || 0;
          if (domainIndex > 0 && DOMAIN_DATA[domainIndex]) {
            currentDimensions = DOMAIN_DATA[domainIndex].dimensions.map((dim, i) => ({
              ...dim,
              levelIndex: analysisData.dimensions && analysisData.dimensions[i] ? this.data.levelList.indexOf(analysisData.dimensions[i].level) : 0,
              level: analysisData.dimensions && analysisData.dimensions[i] ? analysisData.dimensions[i].level : '优秀'
            }));
          }
        } catch {}
      }
      const classIndex = this.data.classList.indexOf(obs.class_name) || 0;
      this.setData({
        recordType: obs.record_type, targetName: obs.target_name, classIndex: classIndex >= 0 ? classIndex : 0,
        observationDate: obs.observation_date ? new Date(obs.observation_date.replace(' ', 'T')) : new Date(),
        observationDateStr: obs.observation_date ? obs.observation_date.slice(0, 16) : '',
        domainIndex, currentDimensions, analysis: obs.analysis || '', images
      });
    } catch (err) { wx.showToast({ title: err.message, icon: 'none' }); }
  },
  chooseImage() {
    const count = 9 - this.data.images.length;
    if (count <= 0) return wx.showToast({ title: '最多9张图片', icon: 'none' });
    wx.chooseImage({ count, sizeType: ['compressed'], sourceType: ['album', 'camera'], success: (res) => this.setData({ images: [...this.data.images, ...res.tempFilePaths] }) });
  },
  removeImage(e) { const images = [...this.data.images]; images.splice(e.currentTarget.dataset.index, 1); this.setData({ images }); },
  async handleSubmit() {
    const { recordType, targetName, classIndex, domainIndex, currentDimensions, analysis, id, linkedId, images } = this.data;
    if (!targetName.trim()) return wx.showToast({ title: '请输入观察对象*', icon: 'none' });
    if (!this.data.classList[classIndex]) return wx.showToast({ title: '请选择班级*', icon: 'none' });
    this.setData({ loading: true });
    try {
      let analysisData = null;
      if (domainIndex > 0 && currentDimensions.length > 0) {
        analysisData = { domainIndex, domainLabel: this.data.domainLabels[domainIndex], dimensions: currentDimensions.map(d => ({ name: d.name, level: d.level })) };
      }
      const data = {
        record_type: recordType, class_name: this.data.classList[classIndex], target_name: targetName,
        observation_date: this.data.observationDate ? this.data.observationDate.toISOString().slice(0, 19).replace('T', ' ') : null,
        content: this.data.content || '', analysis: analysis || '', analysis_data: analysisData ? JSON.stringify(analysisData) : '', linked_id: linkedId || ''
      };
      const IMAGE_BASE = 'https://aixint.cn';
      const existingImages = [], newImages = [];
      images.forEach(img => { if (img.startsWith('http://tmp') || img.startsWith('wxfile://')) newImages.push(img); else existingImages.push(img.startsWith(IMAGE_BASE) ? img.replace(IMAGE_BASE, '') : img); });
      let allImages = [...existingImages];
      if (newImages.length > 0) { for (const img of newImages) { const url = await api.uploadImage(img); allImages.push(url); } }
      if (id) { await api.updateObservation(id, { ...data, images: JSON.stringify(allImages) }); wx.showToast({ title: '更新成功', icon: 'success' }); setTimeout(() => wx.navigateBack(), 500); }
      else { await api.createObservation(data, images); wx.showToast({ title: '创建成功', icon: 'success' }); setTimeout(() => wx.navigateBack(), 1000); }
    } catch (err) { wx.showToast({ title: err.message, icon: 'none' }); }
    finally { this.setData({ loading: false }); }
  }
});
```

#### pages/login/login.js（72行）

```javascript
const api = require('../../utils/api.js');
Page({
  data: { loading: false },
  onLoad() { if (wx.getStorageSync('token')) wx.switchTab({ url: '/pages/home/home' }); },
  onGetUserInfo(e) {
    if (this.data.loading || !e.detail.userInfo) { wx.showToast({ title: '需要授权才能登录', icon: 'none' }); return; }
    this.setData({ loading: true });
    wx.login({ success: (res) => { if (res.code) this.doLogin(res.code, e.detail.userInfo); else wx.showToast({ title: '登录失败', icon: 'none' }), this.setData({ loading: false }); }, fail: () => { wx.showToast({ title: '登录失败', icon: 'none' }); this.setData({ loading: false }); } });
  },
  doLogin(code, userInfo) {
    wx.request({
      url: 'https://aixint.cn/api/auth/login', method: 'POST', header: { 'Content-Type': 'application/json' },
      data: { code, userInfo: { name: userInfo.nickName || '教师', avatar: userInfo.avatarUrl } },
      success: (res) => {
        if (res.statusCode === 200 && res.data.token) {
          wx.setStorageSync('token', res.data.token); wx.setStorageSync('user', res.data.user); wx.setStorageSync('openid', res.data.openid);
          wx.showToast({ title: '登录成功', icon: 'success' }); setTimeout(() => wx.navigateTo({ url: '/pages/home/home' }), 1000);
        } else wx.showToast({ title: res.data.error || '登录失败', icon: 'none' });
      },
      fail: () => wx.showToast({ title: '网络错误', icon: 'none' }),
      complete: () => this.setData({ loading: false })
    });
  }
});
```

#### pages/home/home.js（142行）

```javascript
const api = require('../../utils/api.js');
const formatDate = (dateStr) => {
  if (!dateStr) return '';
  try { const d = new Date(dateStr); d.setHours(d.getHours() + 8); const pad = n => String(n).padStart(2, '0'); return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`; } catch (e) { return dateStr; }
};
Page({
  data: { list: [], page: 1, limit: 20, loading: false, noMore: false, user: null, openid: '', openidMask: '', searchName: '', recordType: '', classFilterIndex: 0, classFilterList: ['全部班级', '小班1班', '小班2班', '中班1班', '中班2班', '大班1班', '大班2班'] },
  onLoad() { const user = wx.getStorageSync('user'), openid = wx.getStorageSync('openid') || ''; this.setData({ user, openid, openidMask: openid ? '*' + openid.slice(-4) : '' }); this.fetchList(); },
  onShow() { this.setData({ page: 1, list: [], noMore: false }); this.fetchList(); },
  async fetchList() {
    if (this.data.loading || this.data.noMore) return;
    this.setData({ loading: true });
    try {
      const params = { page: this.data.page, limit: this.data.limit };
      if (this.data.searchName) params.target_name = this.data.searchName;
      if (this.data.recordType) params.record_type = this.data.recordType;
      if (this.data.classFilterIndex > 0) params.class_name = this.data.classFilterList[this.data.classFilterIndex];
      const res = await api.getObservations(params);
      const newList = (this.data.page === 1 ? res.list : [...this.data.list, ...res.list]).map(item => ({ ...item, observation_date: formatDate(item.observation_date), created_at: formatDate(item.created_at) }));
      this.setData({ list: newList, noMore: res.list.length < this.data.limit });
    } catch (err) { wx.showToast({ title: err.message, icon: 'none' }); }
    finally { this.setData({ loading: false }); }
  },
  onReachBottom() { if (!this.data.noMore) { this.setData({ page: this.data.page + 1 }); this.fetchList(); } },
  onSearch(e) { this.setData({ searchName: e.detail.value, page: 1, list: [], noMore: false }); this.fetchList(); },
  onClassFilterChange(e) { this.setData({ classFilterIndex: e.detail.value, page: 1, list: [], noMore: false }); this.fetchList(); },
  onTypeChange(e) { this.setData({ recordType: e.detail.value, page: 1, list: [], noMore: false }); this.fetchList(); },
  goDetail(e) { wx.navigateTo({ url: `/pages/detail/detail?id=${e.currentTarget.dataset.id}` }); },
  goRecord() { wx.navigateTo({ url: '/pages/record/record' }); },
  handleDelete(e) { wx.showModal({ title: '确认删除', content: '确定要删除这条记录吗？', success: async (res) => { if (res.confirm) { try { await api.deleteObservation(e.currentTarget.dataset.id); wx.showToast({ title: '删除成功', icon: 'success' }); this.setData({ page: 1, list: [], noMore: false }); this.fetchList(); } catch (err) { wx.showToast({ title: err.message, icon: 'none' }); } } }}); },
  logout() { wx.showModal({ title: '提示', content: '确定退出登录？', success: (res) => { if (res.confirm) { wx.removeStorageSync('token'); wx.removeStorageSync('user'); wx.redirectTo({ url: '/pages/login/login' }); } }}); }
});
```

#### pages/detail/detail.js（155行）

```javascript
const api = require('../../utils/api.js');
Page({
  data: { observation: null, linkedList: [], loading: true },
  onLoad(options) { if (options.id) this.loadDetail(options.id); },
  onShow() { if (this.data.observation && this.data.observation.id) this.loadDetail(this.data.observation.id); },
  async loadDetail(id) {
    try {
      const res = await api.getObservation(id); const obs = res.observation;
      let images = obs.images;
      if (typeof images === 'string') { try { images = JSON.parse(images || '[]'); } catch { images = []; } }
      const IMAGE_BASE = 'https://aixint.cn';
      obs.images = (images || []).map(img => img.startsWith('http') ? img : IMAGE_BASE + img);
      let analysisData = null;
      if (obs.analysis_data) { try { analysisData = JSON.parse(obs.analysis_data); if (analysisData.dimensions) { const levelMap = { '优秀': 0, '良好': 1, '一般': 2, '需关注': 3 }; analysisData.dimensions = analysisData.dimensions.map(d => ({ ...d, levelIndex: levelMap[d.level] !== undefined ? levelMap[d.level] : 0 })); } } catch {} }
      if (obs.observation_date) { const d = new Date(obs.observation_date); d.setHours(d.getHours() + 8); const pad = n => String(n).padStart(2, '0'); obs.observation_date = `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`; }
      this.setData({ observation: obs, analysisData });
      if (obs.linked_id) { const linkedRes = await api.getLinkedObservations(obs.linked_id); const pad = n => String(n).padStart(2, '0'); const linkedList = (linkedRes.observations || []).map(item => { if (item.observation_date) { try { const d = new Date(item.observation_date); d.setHours(d.getHours() + 8); item.observation_date = `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`; } catch {} } item.isCurrent = item.id === obs.id; return item; }); this.setData({ linkedList }); }
    } catch (err) { wx.showToast({ title: err.message, icon: 'none' }); }
    finally { this.setData({ loading: false }); }
  },
  goDetail(e) { wx.navigateTo({ url: `/pages/detail/detail?id=${e.currentTarget.dataset.id}` }); },
  goEdit() { wx.navigateTo({ url: `/pages/record/record?id=${this.data.observation.id}` }); },
  generateReport() { const obs = this.data.observation; wx.navigateTo({ url: `/pages/report/report?target_name=${encodeURIComponent(obs.target_name)}&class_name=${encodeURIComponent(obs.class_name || obs.user_class)}` }); },
  continueObserve() { const obs = this.data.observation; wx.navigateTo({ url: `/pages/record/record?linkedId=${obs.linked_id}&targetName=${encodeURIComponent(obs.target_name)}&className=${encodeURIComponent(obs.class_name)}` }); },
  previewImage(e) { wx.previewImage({ urls: this.data.observation.images, current: e.currentTarget.dataset.url }); },
  async handleDelete() { wx.showModal({ title: '确认删除', content: '确定要删除这条记录吗？', success: async (res) => { if (res.confirm) { try { await api.deleteObservation(this.data.observation.id); wx.showToast({ title: '删除成功', icon: 'success' }); setTimeout(() => wx.navigateBack(), 1000); } catch (err) { wx.showToast({ title: err.message, icon: 'none' }); } } }}); }
});
```

#### pages/report/report.js（144行）

```javascript
const api = require('../../utils/api.js');
const { drawRadarChart } = require('../../utils/chart-radar.js');
const { drawTrendChart } = require('../../utils/chart-trend.js');
const HOME_TIPS_TEMPLATES = [
  '建议家长每天安排固定的亲子阅读时间，帮助孩子养成良好的阅读习惯。',
  '鼓励孩子参与简单的家务劳动，如整理玩具、收拾碗筷，培养生活自理能力。',
  '多带孩子进行户外活动，促进大肌肉动作发展，增强体质。',
  '为孩子创造与同龄伙伴交流的机会，帮助提升人际交往能力。',
  '在日常生活中引导孩子观察自然现象，培养科学探究兴趣。',
  '尊重孩子的兴趣爱好，给予积极的鼓励和肯定，增强自信心。'
];
Page({
  data: { loading: true, loadingStep: '正在加载观察数据...', error: null, reportData: null, genTime: '', homeTips: [], canvasWidth: 320 },
  onLoad(options) { const { target_name, class_name } = options; if (!target_name) { this.setData({ loading: false, error: '缺少参数：target_name' }); return; } this.setData({ targetName: decodeURIComponent(target_name), className: decodeURIComponent(class_name || '') }); this.loadReport(); this.setData({ genTime: this.formatNow() }); },
  onReady() { wx.getSystemInfo({ success: (res) => { this.setData({ canvasWidth: Math.min(res.windowWidth - 48, 340) }); } }); },
  async loadReport() {
    this.setData({ loading: true, error: null, loadingStep: '正在加载观察数据...' });
    try { this.setData({ loadingStep: '正在生成AI评语，请稍候...' }); const res = await api.generateReport(this.data.targetName, this.data.className); const tips = this.pickHomeTips(res.domainSummary); if (res.observations) { res.observations = res.observations.filter(o => o && o.observation_date); res.observations.forEach(o => { if (o.analysis_data && typeof o.analysis_data === 'string') { try { o.analysisData = JSON.parse(o.analysis_data); } catch { o.analysisData = null; } } }); } this.setData({ loading: false, reportData: res, homeTips: tips, genTime: this.formatNow() }); setTimeout(() => this.drawCharts(), 100); }
    catch (err) { this.setData({ loading: false, error: err.message || '加载失败，请重试' }); }
  },
  retryLoad() { this.loadReport(); },
  drawCharts() { const { reportData, canvasWidth } = this.data; if (!reportData || !reportData.domainSummary) return; try { drawRadarChart('radarChart', reportData.domainSummary, canvasWidth); } catch (e) { console.error('Radar chart error:', e); } try { if (reportData.observations && reportData.observations.length > 1) { drawTrendChart('trendChart', reportData.observations, canvasWidth + 20); } } catch (e) { console.error('Trend chart error:', e); } },
  levelClass(level) { const map = { '优秀': 'excellent', '良好': 'good', '一般': 'normal', '需关注': 'attention' }; return map[level] || 'normal'; },
  pickHomeTips(domainSummary) { const sorted = [...domainSummary].sort((a, b) => { const score = (l) => { const m = { '优秀': 0, '良好': 1, '一般': 2, '需关注': 3 }; return m[l] || 2; }; return score(a.level) - score(b.level); }); const tips = []; const usedIndices = new Set(); for (const d of sorted) { if (tips.length >= 3) break; const di = d.domainIndex; if (!usedIndices.has(di)) { tips.push(HOME_TIPS_TEMPLATES[di - 1] || HOME_TIPS_TEMPLATES[0]); usedIndices.add(di); } } while (tips.length < 3) tips.push(HOME_TIPS_TEMPLATES[tips.length]); return tips; },
  formatNow() { const d = new Date(); const pad = n => String(n).padStart(2, '0'); return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`; },
  saveReport() { wx.showToast({ title: '报告已生成，可截图保存', icon: 'none', duration: 2000 }); },
  onShareAppMessage() { const { reportData } = this.data; return { title: `${reportData?.childName || ''} 的成长报告`, path: `/pages/report/report?target_name=${encodeURIComponent(this.data.targetName || '')}&class_name=${encodeURIComponent(this.data.className || '')}` }; }
});
```

#### pages/export/export.js（131行）

```javascript
const api = require('../../utils/api.js');
Page({
  data: { exportType: 'single', selectedIds: [], targetName: '', linkedId: '', startDate: '', endDate: '', exportFormat: 'excel', list: [], loading: false, exporting: false },
  onShow() { this.loadMyRecords(); },
  async loadMyRecords() { try { const res = await api.getObservations({ limit: 100 }); this.setData({ list: res.list }); } catch (err) { wx.showToast({ title: err.message, icon: 'none' }); } },
  onExportTypeChange(e) { this.setData({ exportType: e.detail.value }); },
  onFormatChange(e) { this.setData({ exportFormat: e.detail.value }); },
  onInput(e) { this.setData({ [e.currentTarget.dataset.field]: e.detail.value }); },
  onSelectRecord(e) { const { id } = e.currentTarget.dataset; const { selectedIds } = this.data; const index = selectedIds.indexOf(id); if (index > -1) selectedIds.splice(index, 1); else selectedIds.push(id); this.setData({ selectedIds }); },
  selectAll() { this.setData({ selectedIds: this.data.list.map(item => item.id) }); },
  clearSelection() { this.setData({ selectedIds: [] }); },
  async handleExport() {
    const { exportType, exportFormat, selectedIds, linkedId, targetName, startDate, endDate } = this.data;
    const params = { format: exportFormat };
    if (exportType === 'single' || exportType === 'custom') { if (selectedIds.length === 0) return wx.showToast({ title: '请选择要导出的记录', icon: 'none' }); params.ids = selectedIds; }
    else if (exportType === 'linked') { if (!linkedId) return wx.showToast({ title: '请输入串联ID', icon: 'none' }); params.linked_id = linkedId; }
    else if (exportType === 'today') { const today = new Date().toISOString().split('T')[0]; params.start_date = today; params.end_date = today + ' 23:59:59'; }
    if (targetName) params.target_name = targetName;
    if (startDate) params.start_date = startDate;
    if (endDate) params.end_date = endDate + ' 23:59:59';
    this.setData({ exporting: true });
    try { let res; if (exportFormat === 'excel') res = await api.exportExcel(params); else res = await api.exportWord(params); if (res.success) { wx.downloadFile({ url: 'https://aixint.cn' + res.file, success: (downRes) => { wx.openDocument({ filePath: downRes.tempFilePath, success: () => { wx.showToast({ title: '导出成功', icon: 'success' }); } }); } }); } }
    catch (err) { wx.showToast({ title: err.message, icon: 'none' }); }
    finally { this.setData({ exporting: false }); }
  }
});
```

#### utils/chart-trend.js（趋势图，183行）

```javascript
const LEVEL_SCORE = { '优秀': 5, '良好': 4, '一般': 3, '需关注': 2 };
const DOMAIN_COLORS = ['#10b981', '#3b82f6', '#8b5cf6', '#f59e0b', '#ef4444', '#06b6d4'];
const DOMAIN_NAMES = ['健康', '语言', '社会', '科学', '艺术', '学习'];
function parseDate(dateStr) { if (!dateStr) return null; const parts = dateStr.split(/[\sT]/); if (parts.length < 2) return null; const dateParts = parts[0].split('-'); const timeParts = parts[1].split(':'); if (dateParts.length < 3) return null; return new Date(parseInt(dateParts[0]), parseInt(dateParts[1]) - 1, parseInt(dateParts[2]), parseInt(timeParts[0]) || 0, parseInt(timeParts[1]) || 0, 0); }
function drawTrendChart(canvasId, observations, canvasWidth) {
  if (!canvasId || !observations || !observations.length) return;
  const ctx = wx.createCanvasContext(canvasId);
  const width = canvasWidth || 340, height = 310, padLeft = 38, padRight = 15, padTop = 30, padBottom = 30;
  const chartW = width - padLeft - padRight, chartH = height - padTop - padBottom;
  const monthData = {}; const monthlyAvg = {};
  observations.forEach(obs => { if (!obs || !obs.observation_date) return; const d = parseDate(obs.observation_date); if (!d) return; const key = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}`; if (!monthData[key]) monthData[key] = {}; });
  const sortedKeys = Object.keys(monthData).sort();
  sortedKeys.forEach(key => { monthlyAvg[key] = { 1: [], 2: [], 3: [], 4: [], 5: [], 6: [] }; });
  observations.forEach(obs => { if (!obs || !obs.observation_date || !obs.analysisData || !obs.analysisData.dimensions) return; const di = obs.analysisData.domainIndex; if (!di || di < 1 || di > 6) return; const d = parseDate(obs.observation_date); if (!d) return; const key = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}`; if (!monthlyAvg[key]) return; obs.analysisData.dimensions.forEach(dim => { const score = LEVEL_SCORE[dim.level] || 3; monthlyAvg[key][di].push(score); }); });
  const avgData = {}; Object.entries(monthlyAvg).forEach(([key, domains]) => { avgData[key] = {}; for (let i = 1; i <= 6; i++) { const vals = domains[i]; avgData[key][i] = vals.length > 0 ? vals.reduce((a, b) => a + b, 0) / vals.length : null; } });
  const xLabels = sortedKeys.map(k => k.slice(5)); const xCount = xLabels.length;
  ctx.clearRect(0, 0, width, height);
  const yLabelMap = { 1: '需关注', 2: '一般', 3: '良好', 4: '优秀' };
  for (let y = 1; y <= 4; y++) { const yPos = padTop + chartH - (y - 1) * (chartH / 3); ctx.setStrokeStyle('#f3f4f6'); ctx.setLineWidth(1); ctx.beginPath(); ctx.moveTo(padLeft, yPos); ctx.lineTo(width - padRight, yPos); ctx.stroke(); ctx.setTextAlign('right'); ctx.setFillStyle('#ffffff'); ctx.fillRect(padLeft - 30, yPos - 8, 28, 14); ctx.setFillStyle('#9ca3af'); ctx.setFontSize(9); ctx.fillText(yLabelMap[y], padLeft - 4, yPos + 3); }
  ctx.setTextAlign('center'); xLabels.forEach((label, i) => { if (xCount <= 1) return; const xPos = padLeft + (i / Math.max(xCount - 1, 1)) * chartW; ctx.fillText(String(label) + '月', xPos, height - 8); });
  if (xCount >= 1) { for (let di = 1; di <= 6; di++) { const points = []; sortedKeys.forEach((key, i) => { const val = avgData[key][di]; if (val !== null && val !== undefined) { const x = padLeft + (xCount <= 1 ? chartW / 2 : i / Math.max(xCount - 1, 1)) * chartW; const y = padTop + chartH - (val - 2) / 3 * chartH; points.push({ x, y, val }); } }); if (points.length < 1) continue; ctx.beginPath(); ctx.setStrokeStyle(DOMAIN_COLORS[di - 1]); ctx.setLineWidth(1.5); points.forEach((p, i) => { if (i === 0) ctx.moveTo(p.x, p.y); else ctx.lineTo(p.x, p.y); }); ctx.stroke(); points.forEach(p => { ctx.beginPath(); ctx.arc(p.x, p.y, 3, 0, 2 * Math.PI); ctx.setFillStyle(DOMAIN_COLORS[di - 1]); ctx.fill(); }); } }
  const legendY = 8; for (let i = 0; i < 6; i++) { const x = padLeft + i * 50; ctx.setFontSize(8); ctx.setFillStyle(DOMAIN_COLORS[i]); ctx.fillRect(x, legendY, 8, 8); ctx.setFillStyle('#6b7280'); ctx.textAlign = 'left'; ctx.fillText(DOMAIN_NAMES[i], x + 11, legendY + 7); }
  ctx.draw();
}
module.exports = { drawTrendChart };
```

---

## 第五部分：AI辅助开发流程与提示词

### 5.1 AI辅助开发概述

本项目开发过程中全程借助**生成式人工智能**（豆包/MiniMax）进行辅助编程，主要用于：

1. 项目框架和架构设计
2. 数据库表结构设计
3. API路由与业务逻辑设计
4. Canvas图表绘制算法
5. 领域评价体系数据结构设计
6. 代码审查与性能优化

### 5.2 核心AI提示词（关键开发证据）

#### 提示词1：项目初始化与架构设计

```
我想开发一个微信小程序，用于幼儿园儿童观察记录。
功能需求：
1. 教师登录（微信授权）
2. 创建幼儿观察记录（包含领域选择、评级、图片上传）
3. 查看记录列表（支持筛选和搜索）
4. 生成成长报告（包含雷达图和趋势图）
5. 导出记录（Excel/Word格式）

技术要求：
- 前端：微信小程序，原生开发
- 后端：Node.js + Koa2
- 数据库：MySQL

请帮我设计项目结构和核心文件。
```

#### 提示词2：Canvas雷达图绘制

```
我需要在微信小程序中使用Canvas绘制雷达图，展示儿童发展评测结果。
要求：
- 6个维度（健康、语言、社会、科学、艺术、学习品质）
- 5个等级（优秀=5,良好=4,一般=3,需关注=2）
- 需要绘制同心五边形背景、数据多边形、轴线、标签
请用微信小程序的Canvas API实现，附完整代码。
```

#### 提示词3：领域数据结构设计

```
请设计幼儿园儿童发展观察记录的领域评价数据。
基于《3-6岁儿童学习与发展纲要》，设计6个领域：
1. 健康领域 - 生活自理、大肌肉动作、精细动作、安全与健康
2. 语言领域 - 倾听理解、表达交流、早期阅读、文明用语
3. 社会领域 - 自我意识、人际交往、冲突处理、规则意识、归属感
4. 科学领域 - 探究兴趣、数感与认知、逻辑思维、自然感知
5. 艺术领域 - 音乐感受、美术表现、审美情趣
6. 学习品质 - 专注力、任务意识、学习习惯、独立性
每个维度支持4个评级：优秀、良好、一般、需关注。
请用JavaScript对象形式给出数据结构。
```

#### 提示词4：后端API与数据库设计

```
请为幼儿园观察记录系统设计后端API：
功能端点：
- POST /api/auth/login - 微信登录
- GET /api/observations - 获取观察记录列表
- POST /api/observations - 创建观察记录
- GET /api/observations/:id - 获取单条记录
- PUT /api/observations/:id - 更新记录
- DELETE /api/observations/:id - 删除记录
- GET /api/observations/linked/:linkedId - 获取串联记录
- POST /api/export/excel - 导出Excel
- POST /api/export/word - 导出Word
- GET /api/report/generate - 生成成长报告

数据库使用MySQL，请设计SQL建表语句（users表和observations表）。
```

### 5.3 开发流程时间线

| 阶段 | 内容 | AI辅助环节 |
|-----|------|-----------|
| Day 1 | 需求分析、架构设计 | AI辅助完成项目结构设计 |
| Day 2-3 | 完成登录模块 | AI辅助微信授权登录流程 |
| Day 4-5 | 完成首页列表 | AI辅助列表渲染和筛选逻辑 |
| Day 6-8 | 完成记录创建 | AI辅助表单和领域选择数据结构 |
| Day 9-10 | 完成详情页 | AI辅助图片预览和串联逻辑 |
| Day 11-13 | 完成成长报告 | AI辅助Canvas雷达图和趋势图 |
| Day 14 | 完成导出模块 | AI辅助Excel/Word导出 |
| Day 15 | 前后端联调 | AI辅助代码审查 |
| Day 16 | 部署上线 | — |

### 5.4 AI工具使用心得

1. **明确输入输出**：给AI描述清楚要什么，让它给出具体的代码结构
2. **分步骤开发**：不要一次性让AI生成整个项目，分模块开发更高效
3. **代码审查**：AI生成的代码需要人工审查，特别是边界条件
4. **迭代优化**：AI生成的图表代码经常需要根据实际数据调整参数

---

## 第六部分：代码量统计

| 文件类型 | 文件数 | 总行数 |
|---------|-------|-------|
| JavaScript (.js) | 9 | 1,389 |
| 模板 (.wxml) | 6 | 467 |
| 样式 (.wxss) | 7 | 1,248 |
| 配置 (.json) | 7 | ~150 |
| **合计** | **29** | **~3,254** |

**核心代码约2000行**（已收录于本档第四部分）

---

*文档版本：v1.0.0*
*生成日期：2026-05-08*
*项目仓库：https://github.com/forever361/child_info_platform*
